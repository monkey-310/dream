export enum Routes {
  /*Diagnostic Flow Routes */
  Home = "/",
  StudentProfile = "/f/student-profile",
  DiagnosticTest = "/f/diagnostic-test",
  BookingPage = "/f/book-appointment",
  GenerateResult = "/f/generate-result",

  /* Tutor Dashboard Routes */
  UserList = "/t/user-list",

  /*  Auth Routes */
  Login = "/auth/login",
}
